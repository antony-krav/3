{
  "name": "mygreenfriend-bot",
  "version": "0.1.0",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "test:function": "node -e \"require('dotenv').config(); const func = require('./netlify/functions/send-reminders'); const event = { httpMethod: 'GET' }; const context = {}; func.handler(event, context).then(console.log).catch(console.error)\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "telegraf": "^4.12.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "netlify-cli": "^15.0.0"
  }
}